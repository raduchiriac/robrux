import React from 'react';

import Container from '@material-ui/core/Container';

const Dashboard = () => (
  <Container>
    <h2>Dashboard</h2>
    <p>It demonstrates the ability to change the default dashboard and render a custom page instead.</p>
  </Container>
);

export default Dashboard;
